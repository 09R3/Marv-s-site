server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Block access to hidden files/dirs (.git, etc.)
    location ~ /\. {
        deny all;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
